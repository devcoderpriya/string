#include<stdio.h>
#include<string.h>
int main(){
    char ch[]="hello";
    
    int i;
    for( i = 0; ch[i] != '\0'; i++){
        ch[i]= toupper(ch[i]);
    }
    printf("uppercase %s\n",ch);
    return 0;
}
